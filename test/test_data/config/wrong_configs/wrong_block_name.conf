events {
	max_connections 3;
}

http {
	root	servers;

	server {
		listen				127.0.0.1:4242;
		root				servers/test/html;

		location = / {
			index			index.html;
			limit_except	GET DELETE;
		}
	}
}

event { }
